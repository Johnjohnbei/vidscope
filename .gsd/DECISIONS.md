# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | bootstrap | language | Primary implementation language | Python 3.12+ | Best ecosystem for yt-dlp, faster-whisper, ffmpeg bindings, and the official MCP SDK. Stable typing features and stdlib improvements in 3.12 simplify code. | Yes | collaborative |
| D002 | bootstrap | tooling | Python package and environment manager | uv | Faster than pip/poetry, lockfile-driven, reproducible, and installs project tools (like `vidscope` itself) globally via `uv tool install`. Lowest-friction cross-platform option. | Yes | agent |
| D003 | bootstrap | architecture | Project source layout | src layout (`src/vidscope/`) | Enforces installation-as-used testing, prevents accidental imports of uninstalled code, and is the modern Python packaging default. | Yes | agent |
| D004 | bootstrap | cli | CLI framework | Typer | Typed, self-documenting, generates help and completions automatically, composes well with Python type hints. Better developer experience than argparse without the weight of Click subclassing. | Yes | agent |
| D005 | bootstrap | storage | Primary data store for v1 | SQLite with FTS5 | Single file, zero setup, handles thousands of videos, ships with Python, and FTS5 gives us full-text search for free. Meets R005 and R006 without external services. | Yes — migration path to Postgres + pgvector explicitly preserved | collaborative |
| D006 | bootstrap | storage | Database access pattern | Thin repository layer over SQLAlchemy Core | Keeps the data layer swappable. A future move to Postgres + pgvector touches only the repository module; the pipeline stages stay unchanged. Avoids the full ORM to keep queries transparent. | Yes | agent |
| D007 | bootstrap | pipeline | Download / ingestion library | yt-dlp (Python library, not just CLI) | Supports all three target platforms (Instagram, TikTok, YouTube) plus ~1800 others for free. Calling it as a library rather than a subprocess gives us structured metadata and error handling. Known risk: platform API changes occasionally break yt-dlp for a day or two before an upstream patch — S02 will surface a clean error in that case. | Yes | agent |
| D008 | bootstrap | pipeline | Transcription backend | faster-whisper on CPU | 4–5x faster than the reference whisper with identical accuracy, runs on CPU without a GPU, and handles French and English reliably. Default model is `base` or `small` — fast enough for Reels (30s–2min) on consumer hardware. | Yes — GPU backends can be added without touching the caller | agent |
| D009 | bootstrap | pipeline | Frame extraction tool | ffmpeg invoked as a subprocess | The industry standard. No reason to reinvent. The CLI is checked at startup (R009) with an actionable error when missing. Keyframes + fixed-rate sampling, capped at 30 frames per video by default. | Yes | agent |
| D010 | bootstrap | analyzer | Default qualitative analyzer | Pure-heuristic provider (no LLM) | Guarantees zero cost and zero network dependency by default. Heuristics cover transcript length, lexical density, keyword extraction, language detection, topic signals, and a composite relevance score. Good enough to sort and filter; richer judgment is opt-in. | Yes | collaborative |
| D011 | bootstrap | analyzer | Analyzer interface strategy | Pluggable provider interface with env-selected backend (`VIDSCOPE_ANALYZER`) | Ships the seam in M001 so M004 can add NVIDIA Build, Groq, Cerebras, OpenRouter, OpenAI, and Anthropic providers without touching any caller. Satisfies R010 and the user's explicit goal of staying free by default while keeping the upgrade path open. | Yes | collaborative |
| D012 | bootstrap | orchestration | Job queue for the pipeline | Single SQLite table acting as a job queue | No Redis, no Celery, no external broker. A `jobs` table with status (queued, running, done, failed), retry_count, last_error, and a claim/lease pattern is enough for a single-node personal tool. Keeps the monitoring milestone (M003) a pure extension rather than a rebuild. | Yes | agent |
| D013 | bootstrap | observability | Per-stage run records | Every pipeline stage writes a structured `pipeline_runs` row with phase, started_at, finished_at, result, error, retry_count | Makes `vidscope status` (R008) a straight DB read, makes retries idempotent by stage, and gives a future debugging agent a grep-able surface without log diving. | Yes | agent |
| D014 | bootstrap | testing | Test framework and type checking | pytest + ruff + mypy (strict on our code, lenient on third-party) | Standard, well-known, fast feedback. mypy strict mode on our own modules catches the vast majority of refactor regressions without slowing development. | Yes | agent |
| D015 | bootstrap | repo | Git remote and visibility | Private repo at `github.com/Johnjohnbei/vidscope` | Personal tool, not ready for public exposure. Can be flipped to public later with no migration cost. | Yes | human |
| D016 | bootstrap | scope | Platforms supported in v1 | Instagram (Reels and public posts), TikTok, YouTube (videos and Shorts) | Matches the user's primary need without opening the door to the full 1800-site yt-dlp surface (which would drag in paywalls, DRM, and exotic formats). Expansion is trivial later because yt-dlp handles the rest natively. | Yes | collaborative |
| D017 | bootstrap | scope | Milestone boundary for M001 | Pipeline ponctuel end-to-end (single URL), no MCP, no monitoring, no LLM providers | Keeps the first milestone honest: one command, one URL, real proof that ingest + transcribe + frames + analyze + DB + search actually work together. Every later milestone is an extension of a proven socle. | Yes | collaborative |
| D018 | bootstrap | platform | Development platform baseline | Windows 10/11 (dev machine) + cross-platform discipline (macOS, Linux) | Current dev machine is Windows. The tool must not rely on Windows-specific paths, shells, or binaries. All scripts use forward slashes and cross-platform Python APIs. CI, when added, runs on all three. | Yes | agent |
| D019 | M001/S01 | quality | Mechanical enforcement of layering rules | import-linter contract in .importlinter + pytest test that runs `lint-imports` and fails the suite if any layer violates the inward-only dependency rule. | Architecture rules that aren't enforced automatically rot. Discipline doesn't scale across milestones and context windows. import-linter is 5 lines of config + 1 pytest test and catches every violation at test time. Concretely it prevents: cli/ importing adapters/ directly, pipeline/ importing a concrete adapter, domain/ importing anything project-internal, adapters/ cross-importing each other. This is the single most important non-functional check in the project. | Yes — but removing it should require an explicit successor decision. | collaborative |
| D020 | M001/S01 | architecture | Package layout and dependency direction | Seven layers: domain → ports → adapters → pipeline → application → interfaces (cli/mcp) + infrastructure as composition root. Dependencies point inward only. | Concrete structure implementing D019. domain/ has zero project imports (pure Python, no SQLAlchemy, no Typer). ports/ imports only domain/. adapters/ import domain/ and ports/. pipeline/ imports domain/ and ports/ (never a concrete adapter). application/ (use cases) imports pipeline/, ports/, domain/. cli/ (and future mcp/) imports application/ only. infrastructure/ is the single place that composes adapters and wires the dependency graph at startup. This gives us: (1) domain tests that run in milliseconds with no I/O, (2) adapter tests that target one layer at a time, (3) a single composition root where all dangerous wiring happens, (4) trivial substitution for tests and future backends. | Yes — but any change must preserve the inward-only dependency rule. | collaborative |
| D021 | M001/S01 (contract), exercised in S02-S06 | architecture | Pipeline stage contract and failure handling | Every stage implements a Stage protocol with execute(context) -> StageResult, is_satisfied(context) -> bool (for idempotent resume), and raises typed domain errors. PipelineRunner persists a pipeline_runs row per stage invocation in the same transaction as the stage's domain write. | R007 requires single-command end-to-end ingestion with partial-success recovery. R008 requires inspectable pipeline state. The only way to guarantee 'no half-written rows' and 'resume from last successful stage' is: (a) each stage declares whether its output already exists via is_satisfied(), (b) each stage invocation is transactionally bundled with its pipeline_runs row, (c) errors are typed per domain so the runner can route them (IngestError → retry with backoff; TranscriptionError → fail-fast with user-actionable message; StorageError → abort). A bare try/except in the CLI would lose all of this. The Stage protocol lives in ports/, concrete stages in pipeline/, adapters are injected. | Yes | agent |
| D022 | M001/S01 | storage | Media storage abstraction | MediaStorage port with LocalMediaStorage adapter (filesystem) shipping in S01. Stages never touch pathlib directly for video/frame files — they receive a MediaStorage and call store(key, source_path) / resolve(key) / exists(key) / open(key). | Cleanly isolates the filesystem from pipeline logic. Lets us swap to S3/MinIO/GCS later without touching any stage. Also makes tests trivial: InMemoryMediaStorage for unit tests, tmp_path-backed LocalMediaStorage for integration. Cost is ~50 lines in S01 vs. a refactor across every stage if we do it later. Storage keys are stable strings (e.g. 'videos/{video_id}/media.mp4', 'videos/{video_id}/frames/0000.jpg') so the physical layout is an adapter concern, not a domain concern. | Yes | collaborative |
| D023 | M001/S01 | quality | Mechanical enforcement of layering rules | import-linter contract in .importlinter + pytest test that runs `lint-imports` and fails the suite if any layer violates the inward-only dependency rule. | Architecture rules that aren't enforced automatically rot. Discipline doesn't scale across milestones and context windows. import-linter is ~10 lines of config + 1 pytest test and catches every violation at test time. Concretely it prevents: cli/ importing adapters/ directly, pipeline/ importing a concrete adapter, domain/ importing anything project-internal, adapters/ cross-importing each other. This is the single most important non-functional check in the project. | Yes — but removing it should require an explicit successor decision. | collaborative |
| D024 | M001/S02 planning, after S01 delivered the hexagonal socle | architecture | External API exposure strategy | MCP server in M002 only. No HTTP/REST API planned for M001-M005. The use cases in vidscope.application are already the single API boundary — MCP and a future HTTP layer would both wrap the same use cases without touching pipeline/adapters/domain. | VidScope is a single-user local tool (R032 out-of-scope for multi-user web service). MCP covers the "AI agent calls a local tool" use case natively with zero HTTP plumbing, no auth, no CORS, no exposed ports. The hexagonal architecture shipped in S01 already separates business logic (application/ use cases) from interface (cli/ package), so adding HTTP or gRPC later is a pure additive change — a new `vidscope.http` package alongside `vidscope.cli`, consuming the same use cases via the same Container. Decision is cheap to revise: if a real HTTP need surfaces in M003+, we add a dedicated milestone without refactoring anything. | Yes — explicitly. Adding an HTTP API later is a new package, not a refactor. | collaborative |
| D025 | M001/S02/T03 | pipeline | IngestStage.is_satisfied() behavior for resume-from-failure | Always returns False in S02. Idempotence is enforced at the DB level via `upsert_by_platform_id`, not via an is_satisfied short-circuit. Re-running `vidscope add <url>` re-downloads the media but does not duplicate the videos row. S06 will revisit this with a proper probe-before-download optimization if the re-download cost becomes painful. | True is_satisfied-based short-circuit for ingest would require extracting the yt-dlp platform_id WITHOUT downloading (an `extract_info(download=False)` call), which means either extending the Downloader port with a probe() method or doing a two-phase extract-then-download dance. Both add complexity the first implementation shouldn't carry. The DB-level idempotence (UNIQUE platform_id + ON CONFLICT DO UPDATE) already guarantees "no duplicate rows" which is the important invariant. Re-downloading on resume is suboptimal but safe. S06 (end-to-end wiring) will add probe() to the Downloader port once we have enough real-world data on how long re-downloads actually take. | Yes — explicitly planned for S06 review. | agent |
| D026 | M001/S02/T06, after the first live ingest test revealed the mismatch | scope | Target content profile for ingest tests and performance tuning | Short-form vertical content only: YouTube Shorts (< 60s), Instagram Reels (< 90s), TikTok videos (typical 15-60s). Long-form YouTube, podcasts, and anything over ~3 minutes is out of scope for the primary use case. | The user's veille loop is about scanning short-form content from the three platforms — that's where the signal-per-minute ratio is high enough to justify automated transcription and analysis. Long-form content has different tradeoffs: transcription time dominates, analysis value per minute is lower, and the user wouldn't pick vidscope as the primary tool for it. Calibrating the integration tests and the S03 transcription performance targets around short-form content keeps the tool's identity sharp. Big Buck Bunny (10 minutes) was a poor choice as the YouTube smoke URL because it's representative of nothing the user actually ingests. | Yes — if the user's needs expand later, the short-form focus can be relaxed without architectural changes. | human |
| D027 | M001/S02 closure — user correction after the slice landed with the inverted priority | scope | Platform priority order for VidScope | Instagram is the primary target platform (highest user value), TikTok second, YouTube third. All technical decisions, test priorities, integration test ordering, error message wording, and feature scoping should reflect this order. | User explicitly stated the priority during S02 closure. The current S02 implementation accidentally inverted this — YouTube and TikTok were validated as passing while Instagram was xfailed, which sends the wrong signal about which platform matters most. Instagram is also the most fragile platform technically (yt-dlp now requires authentication for public Reels as of 2026-04 — see R025), which means the platform that matters most is also the one most likely to break upstream. This makes cookie-based authentication (R025, currently deferred to M005) much more important than its current scoping suggests. The priority order is: 1) Instagram Reels, 2) TikTok videos, 3) YouTube Shorts. All are short-form per D026. | Yes, but only with explicit user input — the order reflects real-world usage priorities, not technical convenience. | human |
| D028 | M006 bootstrap | architecture | Creator becomes a first-class domain entity with its own table and FK from videos | New `creators` table with UNIQUE (platform, platform_user_id); `videos.author` kept as denormalised cache but `videos.creator_id` FK becomes the source of truth. Backfill script migrates existing rows. | Denormalised `author` string loses all tracking context. Every qualitative improvement planned in M007–M011 depends on a stable creator identity: mention attribution (M007), creator-level velocity (M009), collections and facetted search by creator (M011). yt-dlp already exposes `uploader_id`, `uploader_url`, `channel_follower_count`, `uploader_thumbnail` at zero extra cost — this is a structural cleanup, not a feature acquisition. | Yes — if M006 ships with bugs we can fall back to the denormalised column until fixed. | collaborative |
| D029 | M007 bootstrap | architecture | Hashtags, mentions, links, music are side tables (many-to-many via junction or FK), not JSON blobs on videos | `video_metadata` 1:1 with videos (description + location), separate `hashtags`, `mentions`, `music_tracks`, `links` tables keyed by video_id. | Side tables stay queryable by SQL facets (`WHERE hashtag = X`) without JSON-path extensions. JSON blobs would force every search to re-parse and defeat FTS5. The write cost is negligible (a few INSERT rows per ingest), the read cost collapses to a normal JOIN. Aligns with D005 (SQLite + FTS5 as primary store). | Yes — additive migration, can be collapsed into JSON later if row count becomes a problem, but very unlikely at personal-tool scale. | agent |
| D030 | M008 bootstrap | dependencies | Local OCR via `rapidocr-onnxruntime` (CPU ONNX, FR+EN) | Add `rapidocr-onnxruntime` as an optional dependency (extra `vidscope[vision]`). Model downloaded on first use, cached under platformdirs cache dir. | Matches D010's zero-cost default: no API key, no network, no per-request cost. ONNX CPU runs on user hardware; 50 MB model is acceptable. RapidOCR handles Latin script including French reliably. Tesseract was considered but requires a system-level binary which breaks D018 (cross-platform install simplicity). Optional extra so core VidScope still installs without the vision dep. | Yes — seam is the `OcrEngine` port; a Tesseract or cloud-OCR adapter could be added later without touching callers. | agent |
| D031 | M009 bootstrap | storage | Engagement stats are append-only time-series, not current-value columns on videos | New `video_stats` table with `(video_id, captured_at, view_count, like_count, comment_count, share_count, save_count)`, indexed on `(video_id, captured_at)`. Repository rejects UPDATE — INSERT only. Derived metrics (velocity, engagement_rate) computed on read in `domain/metrics.py`. | Vélocité et tendance sont le vrai signal de veille. Un snapshot overwrite-style perd l'historique et rend tout scoring temporel impossible. Append-only garantit la reproductibilité ("quelle était la view-count le 12 mai à 14h?"), permet les re-traitements et évite toute perte de données. Le coût disque est trivial (quelques kilo-octets par vidéo par refresh). | Non — un retour à un modèle overwrite détruirait l'ensemble des cas d'usage analytiques de M009/M011. | collaborative |
| D032 | M010 bootstrap | migrations | Analysis V2 fields ajoutés en nullable, ancien `score` préservé | Migration 009 ajoute `information_density`, `actionability`, `novelty`, `production_quality`, `sentiment`, `is_sponsored`, `content_type`, `reasoning` — toutes NULLable. Ancien `score` reste. Analyses existantes restent valides; ré-analyser populate les nouveaux champs. | Rétro-compatibilité totale. Zéro downtime de ré-analyse. Les anciens rows "marchent" (summary + score) tant qu'on ne les ré-analyse pas. Les LLM providers V2 émettent le nouveau schéma; l'heuristique V2 aussi. Si un provider V1 persiste (stub), ses rows restent avec nulls sur les champs V2 — acceptable. | Oui — si le schéma s'avère mal choisi, V3 peut se chainer de la même manière. | agent |
| D033 | M011 bootstrap | architecture | Tracking / tags / collections vivent dans des tables séparées, jamais sur `videos` | `video_tracking` 1:1 (status, starred, notes), `tags` + `video_tags` M:N, `collections` + `collection_items` M:N. Re-ingesting a video never touches these. | L'overlay workflow est un concern utilisateur, pas un concern contenu. Mixer les deux colonnes sur `videos` polluerait l'entité domain et risquerait d'effacer des annotations utilisateur lors d'un ré-ingest idempotent. La séparation garantit que le cycle de vie du contenu (muté par pipeline) et celui des annotations (muté par user uniquement) n'interfèrent jamais. | Non — tout retour en arrière exposerait l'utilisateur à une perte d'annotations. | collaborative |
| D034 | M011 bootstrap | interop | Formats d'export supportés : JSON canonique (v1), Markdown avec YAML frontmatter (Obsidian-ready), CSV plat (Airtable-ready) | Un exporter par format. JSON est la source de vérité; Markdown et CSV sont des projections. Schéma JSON versionné (v1) et figé via `docs/export-schema.v1.md`. | Couvre Notion (JSON import), Obsidian (Markdown + frontmatter), Airtable / Excel / Google Sheets (CSV). Trois formats suffisent pour 95% des outils de veille sans coder d'intégration dédiée. Versioning du schéma JSON protège les utilisateurs downstream contre les breaking changes. | Oui — un nouvel exporter (e.g. Notion API direct) est un ajout additif; le schéma v1 ne change jamais en breaking. | collaborative |
